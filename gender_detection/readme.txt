This is to showscase utilizing a pretrained SSL model (SOTA) to detect the gender of the speaker in the audio clip, using the HuggingFace library. 

The model only outputs one classification per clip, so if your clip has both male and female, it still outputs one. 
